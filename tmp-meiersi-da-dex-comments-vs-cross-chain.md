# Temporary: @meiersi-da DEX review comments, assessed against `docs/reference-architectures/cross-chain-stablecoin.md`

Temporary working file. Do not commit. Every review comment by `@meiersi-da` on PR #198 and PR #83 is on `docs/reference-architectures/dex.md`, so all of them are DEX comments. Each row gives the verdict for the cross-chain stablecoin proposal, the reason, and where the change landed.

Verdicts: **Yes** applied; **Partial** applied in part, with the reason; **No** not relevant; **Already addressed** the document already did this.


## PR #198 (https://github.com/OpenZeppelin/canton-specs/pull/198)

| # | Comment | `dex.md` line | Simon's comment | Relevant? | Why | Applied as |
|---|---|---|---|---|---|---|
| 1 | [r4015185628](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015185628) | 31 | why do you need committed allocations? it seems a non-committed one would work as well with the extra caveat that the backend should blacklist traders that create too much contention in the batch settlement due to concurrent withdraws. Could it be that you wanted to write "two iterated allocations" ? | **No** | The DEX question is whether the output amount must be pinned. Here the lock attestation fixes the amount, so a committed receive-side allocation is the right primitive and section 1 already says why. | - |
| 2 | [r4015474413](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015474413) (reply) | 31 | actually upon rereading the text it seems to me that committed, short-lived allocations are a good idea to avoid deliberate contention attacks on the batched settlement. | **No** | Same thread. Contention attacks on a batch do not arise: the sender legs are funded by the mint inside the settlement, so no trader-side committed funds are at stake. | - |
| 3 | [r4015191302](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015191302) | 38 | ```suggestion interfaces, so it interoperates with any conformant token registry. Reusable Access Control and Pausable packages provide optional governance and ``` | **No** | No equivalent sentence. The controls here are D1 to D4 and are not optional add-ons. | - |
| 4 | [r4015198075](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015198075) | 47 | ```suggestion template, one settlement boundary, one swap entry point - so authorization flow and ``` | **No** | Wording fix for a sentence this document does not contain. | - |
| 5 | [r4015211071](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015211071) | 58 | Any chance to reorder the sections so that the party topology is explained (or previewed) before you refer to these parties? | **Yes** | Section 1 named the messaging gateway, the instrument admin, the bridge relayer, the attester set, and the custodian account before section 2.1 defined any party. | Section 1: new paragraph previewing the parties with links to sections 2.1 and 2.3. |
| 6 | [r4015247104](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015247104) | 60 | I remember we talked about allowing both user-submitted trades and operator submitted trades. The user submitted ones come at the cost of lower throughput, but avoid front-running concerns. You have a great explanation below why you don't do that. I haven't checked whether you call out the alternative option of a lower-throughput, but no front-running pool. If you don't then consider calling it out, and consider calling out that it requires moving the compliance checks on-ledger. | **No** | The alternative is about front-running and throughput of a priced venue. A payment rail has no ordering-sensitive price, and its compliance checks already sit on-ledger. | - |
| 7 | [r4015250843](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015250843) (reply) | 60 | > single-synchronizer identity. what do you mean by this? | **Yes** | The scope table used the equally opaque phrase 'cross-synchronizer identity'. | Section 1.2: out-of-scope cell now says 'parties or credentials hosted on another synchronizer'. |
| 8 | [r4026319032](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4026319032) (reply) | 60 | > @meiersi-da in the alternative option, I believe there would still be potential for frontrunning, by the participant nodes the users are submitting through, although lower. Indeed. I overlooked this. As you point out though there may be a stronger trust relationship in place. | **No** | Reply within the front-running thread; no analogue. | - |
| 9 | [r4015259305](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015259305) | 61 | > the pool holdings parties why plural? | **Yes** | Same class of defect: the attesters were singular in 1.1 (D1), 2.2, 3.3 and 3.6 but an N-of-M quorum in 1.3, 2.3 and 6. | D1 in 1.1 and 3.6, the 2.2 table, and 3.3 intro and step 2 now say N-of-M quorum consistently. |
| 10 | [r4015270450](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015270450) | 69 | ```suggestion \| Token Standard V1 \| Mixed-version settlement and V1-specific allocation paths. The design targets the V2 APIs. \| ``` | **Yes** | The scope table has the same Token Standard V1 out-of-scope row, worded less precisely. | Section 1.2: row reworded to the suggested text, naming CIP-0056 as V1. |
| 11 | [r4015400436](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015400436) | 146 | Governance can also refer to off-ledger actions taken to govern the venue. Consider switching to "Venue Validation" or "Decentralized Venue Validation" or "Decentralized Venue Operations". Also a style suggestion: consider defining short abbreviations, e.g., `dvv` and `vo` so that you can easily write sentences of the form "The `dvv` party validates that actions initiated by the `vo` party." Compared to your long-form constants, I personally do find this easier to read and lighter on the eye. | **Yes** | The 'Governance' ambiguity does not apply, but the readability point does: the party names 'bridge relayer' (37 uses), 'gateway admin' (23), 'pause authority', 'trusted-issuer list admin' and 'lawful-process authority' repeat in every table row and diagram note. First assessed as not relevant because the document has no long-form constants; corrected on review. | Section 2.1 roles table gains a Code column (`br`, `ga`, `pa`, `tla`, `lpa`); the codes replace the names in tables and mermaid diagrams, running text keeps the names. `wa` for the wTOK admin was considered and not adopted. || 12 | [r4015491179](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015491179) | 149 | Another aspect that is missing here is a clear distinction between organizations, parties, and nodes. What matters is that the `dvv` party is hosted across multiple independent organizations, so that f + 1 of them would have to collaborate for the guarantees checked by the `dvv` party to be broken. Likewise, it matters that some of these organizations also act as auditors for the settlement batches to detect front-running. The allocations in the batches should be ordered by the record-time of the transactions that created them.  Note that there's though another operational consideration here: what should the backend do if settling a batch fails? In particular, what to do if it times out because one of the trader's nodes does not confirm in time?  I'd suggest that the resubmission removes the trades from that trader *and* calls out the party-ids whose trade were removed and refers to the record-time as of which the first batch submission including the trader failed. This way the auditor can in principle check that the there indeed was a time-out for that trader, and it is OK for its trades to be left-out. In practice I'd expect that kind of failure to be rare and would just check that trader's are not too often blacklisted by the operator backend. This leaves a tiny option to do MEV by targeted blacklisting of a trade and adding one's own trade in a later batch, betting on the trader to resubmit the trade. | **Yes** | The design never named the distinction between organizations, parties and participants, and had no failure row for a settlement that times out because a recipient's participant does not confirm. | Section 2.3 'Background' defines organization, party and participant. Section 4.4 gains the timeout row with resubmission without the offending leg. |
| 13 | [r4026298202](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4026298202) (reply) | 149 | The venue would learn via the completion on its Ledger API that the timeout happened and which party's confirmation timed out. This information is only available to the submitter. | **Yes** | Folded into the timeout row: only the submitter learns which party timed out. | Section 4.4 timeout row. |
| 14 | [r4015335254](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015335254) | 164 | This seems to be an independent organization, but not one that needs to be represented as a party in the Daml workflows. | **No** | This design has no on-ledger auditor party. Its one mention of an auditor (matching releases to burns) is already an off-ledger actor. | - |
| 15 | [r4015507481](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015507481) | 165 | > Separating the issuer from the venue governance   allows future delegation of LP-token issuance to a regulated third-party   custodian. Consider moving this out of scope. It doesn't seem difficult to do for the ones that want to do it, but saves complexity in the default case. | **No** | No LP token or issuer separation exists here. | - |
| 16 | [r4015682487](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015682487) (reply) | 165 | Actually I now do see a reason as to why to manage a party like this. I would call it the "LP Token Admin" as it is the party that administrates the ownership registry for the LP tokens. Managing that separately allows decoupling the implementation of that registry from the implementation of the trading venue.  Thereby reducing complexity in the implementation, security audit, and operations (in particular upgrades). | **No** | The instrument admin is already called 'wTOK admin', and there is a single instrument, so the registry-decoupling point is already the shape of the design. | - |
| 17 | [r4015516056](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015516056) | 176 | ```suggestion - **Trader** - an end-user authoring swap `Allocation`s from their wallet: ``` | **No** | The roles table defines roles without articles; the phrase does not occur. | - |
| 18 | [r4015516904](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015516904) | 179 | ```suggestion - **Liquidity Provider (LP)** - an end-user funding the pool: at provision ``` | **No** | As above. | - |
| 19 | [r4015362077](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015362077) | 182 | Every decentralized party costs in their setup and maintenance. Now that we have the decentralized `VENUE_GOVERNANCE` party, I'd consider using it also for holding the pool's reserves and for issuing the LP tokens. Simpler w/o loss of trustworthiness. | **Yes** | This design has more separately decentralized parties than the DEX: attesters, relayer, pause authority, wTOK admin, Custodian, plus four single-organization roles. Nothing said which could be consolidated. | Section 2.3 'Minimal deployment' maps organizations to parties and names the two separations that must hold. |
| 20 | [r4015529352](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015529352) | 194 | > at a latency and cost premium, the validation runs in parallel, so that latency premium is low in general. Not sure I'd call it out like this. Likewise the cost premium is rather small, as confirmation responses don't cost traffic. | **Yes** | The pause authority row justified threshold 1 with 'a quorum would slow it down', the claim Simon disputes. | Section 2.3: pause row now says a threshold adds little latency but removes self-submission, and an approval workflow adds a round; the multi-hosted bullet states the premium is small. |
| 21 | [r4015549628](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015549628) | 195 | This seems like architectural background. Consider pushing it into the educational framing (which you might also rename to "Background"), so that the architecture itself is presented clearly and succinctly. | **Yes** | Section 2.3 opened with the same architectural background (confirmation thresholds, three routes) mixed into the decision. | Section 2.3 split into a 'Background' lead and a 'Decision' lead, matching the 'Decision' convention used in 3.4. |
| 22 | [r4015542429](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015542429) | 200 | I think this table would profit from distinguishing between the on-ledger parties and the operating organizations. I'd suggest to build a table that has one cell per party x org combination, and calls out what the org does for the party. | **Yes** | The posture table listed parties only and omitted the wTOK admin and the Custodian, the two roles the prose called critical. | Section 2.3 table rebuilt with 'Target posture' (parties and organizations), 'Who submits in its name' and 'Why', and rows added for the wTOK admin, the Custodian, and the gateway and list admins. |
| 23 | [r4015570943](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015570943) | 202 | Consider calling out that the covalidation services may also run the audit backend. I'd also suggest to add a background section on "Covalidation" referring to the [proposed CIP](https://lists.sync.global/g/cip-discuss/topic/cip_0120_traffic_based/120502087) and the business offerings that are available on the network, and that users of a venue can validate the co-validation via the topology state. | **Yes** | The multi-hosted route named no providers and no way for users to verify it. | Section 2.3 multi-hosted bullet: covalidation providers, the CIP-0120 discussion, Canton Coin and cBTC precedent, and verification from topology state. |
| 24 | [r4015637263](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015637263) | 203 | Note: I'd also expect the operator to serve as a validator for the `dvv` party. In general, the simplest topology that I see as viable is: orgs: - one venue operator - two venue validators    - taken from standard covalidation service offerings   - at least one of them offers to also run the audit backend   - both of them only deploy venue.dars audited by third-party auditors (this is important to avoid new choices or choice implementations being deployed that abuse the authority of the `dvv` party) parties: - `dvv` party hosted with 2/3 confirmation rights across venue operator participant node and the two venue validator participant nodes - `vo` party hosted on venue operator participant node What is not clear in this model is which orgs would get pause authority and how unpause should work. I'd expect that in the simplest model both of these would be given to just the venue operator. That matches how TradFi works AFAIK. It might make sense for operational redundancy to use a different party owned by the venue operator for the pausing, but I'm not sure about that. Also consider distinguishing between the relevant structure of the network (e.g., there are separate instrument admins) from the structure that you are newly introducing (the one described above). For example, extend your background section with the relevant information about instrument admins and the assumption that they support CIP-112, so that this section here becomes smaller and focus on the actual changes. | **Yes** | Simplest viable topology and pause ownership were unstated. The 'separate network structure from new structure' part does not apply: the rail introduces its own registry rather than reusing instrument admins. | Section 2.3 'Minimal deployment'; pause row names who holds it and that unpause is the same hosts. |
| 25 | [r4015735642](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015735642) (reply) | 203 | There's one aspect that seems to be missing here: which orgs would control how the configuration of the venue changes? For CC, these are the SV right owners that use on-chain governance votes to effectuate config changes. Some but not all of the SV right owners are also SV node operators that host the `dso` party. The SV node operators correspond to the "venue validators" here. What's missing is the identification of the orgs that acts as "venue governors". Do you have a good idea how to structure and explain this? | **Yes** | Who changes configuration was implicit. | Section 2.3 table: submission column names the gateway admin and list admin as configuration changers, and the wTOK admin's rotations as its own externally signed transactions. Attester admission stays an open question in section 6, as before. |
| 26 | [r4015750667](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015750667) | 283 | consider calling out the metadata key. Something like `dex.reference.openzeppelin.com/min-out-amount` could work. Once this is used often, a CIP might also standardize these parameters. | **Yes** | The design relied on leg metadata to let a recipient match events to a lock (2.2, 4.1, 4.6) without naming the key. | Section 3.1 step 2 names `bridge.reference.openzeppelin.com/lock-nonce`; section 4.6 matches on it. |
| 27 | [r4015756662](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015756662) | 286 | A stale quote should be aborted using `V2.Allocation_Cancel`, so that the funds are released early. Consider calling that out in some place. | **Yes** | Dead flows (expired attestation, already-credited lock) were left to lapse at the deadline. | Section 4.4 'Cancelling a dead flow early' and the duplicate-lock row now use `Allocation_Cancel`. |
| 28 | [r4015765162](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015765162) | 304 | ```suggestion submission will pass operator-defined checks. The ledger will see ``` | **No** | The word 'arbitrary' appears here only in 'arbitrary expropriation', a different sense. | - |
| 29 | [r4015796715](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015796715) | 305 | This raises a concern wrt auditing: for auditing to work well it should be such that all allocations above the min amount are handled in the order that they are created. Thus the cancellation of an allocation that has enough funds, but does not pass compliance checks must also be shown on-ledger, so that the operator commits to it and can be held accountable. An alternative setup is to appeal to the existing off-ledger audit processes that are used for TradFi institutions. In that case it would be an independent auditor that checks their tx logs from time to time. I have no strong opinion wrt how to best solve this. However I think it is important that the target requirement is made clear and the design consistently solves for it. | **Yes** | A withheld compliance attestation left no on-ledger trace, so a denial was not attributable; the requirement was unstated. | Section 3.6 D1: new paragraph on recording denials on-ledger through a cancel with the denial reference, or in the off-ledger log, as a stated policy. |
| 30 | [r4015809068](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015809068) | 333 | Actually: with the venue operator driving settlement: why do you need an on-ledger pause? | **Yes** | The rail is relayer-driven, so the same question applies: why an on-ledger pause? | Section 2.3 pause row: the pause is on-ledger and outside the relayer so a compromised relayer can be stopped. |
| 31 | [r4015895182](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015895182) (reply) | 333 | perhaps we need a separate party to ensure that the pausing can be done if the operator was compromised. | **Yes** | Same thread; the separate pause party is exactly the answer given. | Section 2.3 pause row. |
| 32 | [r4015859987](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015859987) | 357 | AFAIU, the liquidity provisioning will touch the same state that is touched by trade settlement. It should thus also happen as part of batch settlement or not? Consider calling this out in the right place. Here or perhaps a separate "Performance and Scalability" section. | **Partial** | The analogue is state shared between flows. Inbound credits share the credited-lock registry; rotations of the attester and credited-lock registries contend with in-flight settlements; redemptions share nothing. | Section 4.5: paragraph on batching credits under one settlement id and on rotations contending; states that the outbound path has no shared contract. |
| 33 | [r4015873753](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015873753) | 411 | missing:     RegB-->>Wallet: iterated Allocation (receive B) | **Partial** | No missing return arrow was found, but the inbound diagram omitted that the delegated accept consumes the request, which the text states. | Inbound diagram: the accept arrow now says it consumes the request. |
| 34 | [r4015916135](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015916135) | 416 | Another item to mention in the right place: management of the `Holding` UTXOs in the pool. AFAIS, what we want is that the pool funds are always held in a single Holding referenced from the `Pool` contract. | **No** | There are no standing pool reserves on Canton; the sender holdings are created by the mint inside the settlement transaction (and section 3.2 now prefers the `cip-112/mint` account, which removes them entirely). | - |
| 35 | [r4015926161](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015926161) | 424 | Consider marking which steps happen in their own Daml tx. | **Yes** | Only the settlement transaction was drawn as a block; the gateway and delegated-accept transactions were notes. | Both diagrams: every transaction is a `rect` block with its submitter in the note. |
| 36 | [r4015930525](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015930525) (reply) | 424 | e.g., extending the blue background to over the whole tx. | **Yes** | Same thread. | As above. |
| 37 | [r4015938121](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015938121) (reply) | 424 | You could use text color to show privacy. Or alternatively using a grouping like in CIP-112: https://github.com/canton-foundation/cips/blob/main/cip-0112/cip-0112.md#4231-alices-allocations-tx2 That ones is probably not doable in mermaid though. | **No** | Mermaid cannot express per-party visibility well, as the comment itself notes; the 2.2 visibility table carries that information here. | - |
| 38 | [r4015966029](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015966029) | 434 | I believe you'll want the UI to compute the exact amounts, as otherwise the operator learns the size of a potential trade ahead of time. | **No** | There is no quote and the amount is public on the external chain. | - |
| 39 | [r4015970688](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015970688) | 438 | clarify that the trader uses the UI to submit a single transaction that does all of these actions. | **Yes** | Step 4 read as if the mint was a prior step by the admin, and 5.1 billed it as a separate transaction. | Section 3.1 step 4 and section 5.1 first bullet: the mint runs inside the settlement transaction. |
| 40 | [r4015979056](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015979056) | 449 | consider combining these two steps, so that it is clear that it's a single transaction. | **Yes** | Steps that are one transaction should read as one. | Section 3.1 step 4 folds the mint in; new 'Fewer relayer transactions' paragraph explains why steps 2 to 4 stay separate and that they may be collapsed. |
| 41 | [r4015998443](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4015998443) | 455 | The section here does not distinguish between what the venue's contracts do (e.g. compute the right amounts and initiate allocation settlement), and what are the effects of that. I'd suggest to distinguish, so that readers are clear that the information about the settlement comes automatically from the registries via their token and allocation implementation. | **Yes** | Step 4 did not separate what the rail's contracts decide from what the registry's implementation reports. | Section 3.1 step 4: the rail's contracts decide the amounts, the registry's settlement implementation emits the events and creates the holding. |
| 42 | [r4016027698](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016027698) | 464 | Do you think it would be possible to use the same `SettlementInfo` for all trades (or at least all trades that land in the same settlement batch)? I guess that should be possible. The trick is to use different `transferLegIds`. If yes, then that allows the traffic efficient option of allocating the pool legs in a single allocation per instrument, and to settle all of the allocations in a single batch settlement. | **Yes** | Batching several payments needs one settlement id with distinct transfer leg ids; the design promised batching without saying so. | Section 3.1 step 2 (relayer chooses the id, one leg id per payment) and 5.1. |
| 43 | [r4016046577](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016046577) | 469 | ```suggestion id). The extra round-trip is the price of the operator or pool holdings party never taking custody of in-flight trader funds. ``` AFAIS, it's not the "no custody" requirement, but the batch settlement (i.e., high throughput) that requires the separate allocation. Furthermore, the separate allocation also allows for registries to run multi-step approval processes for creating allocations, which enables a smooth integration with TradFi systems. | **Yes** | The 'Settlement over a direct mint' list did not give the strongest reason for a separate allocation step: it lets an account provider run its own approval before the leg commits. | Section 3.1: new bullet in that list. |
| 44 | [r4016053380](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016053380) | 477 | one atomic Daml transaction for multiple trades | **Yes** | Say plainly that one transaction settles many payments. | Section 3.1 step 4. |
| 45 | [r4016059883](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016059883) | 488 | see my comment wrt dealing with timeouts due to trader-hosting participant nodes not confirming in time | **Yes** | Timeout handling. | Section 4.4 timeout row. |
| 46 | [r4016064770](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016064770) | 491 | consider using `transferLegId` to enable efficient pool allocation and settlement (see above) | **Yes** | The reconciliation matched on the settlement id, which batching makes non-unique. | Section 4.6 matches on the leg's metadata key. |
| 47 | [r4016072443](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016072443) | 512 | Do we need that? It makes automated audits more complicated. | **No** | No pre-deadline cancel by the recipient exists; committed allocations become withdrawable only after the deadline, and the preapproval can be archived at any time, which section 4.3 already covers. | - |
| 48 | [r4016093162](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016093162) | 559 | Redemption requests also need to be batch settled as they touch that same UTXOs. Consider calling out that request type as well in the section explaining the main flows. | **No** | A burn consumes only the holder's own holding and creates a claim, so redemptions share no contract with credits. Section 4.5 now states this. | - |
| 49 | [r4016119411](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016119411) | 624 | consider calling out the option of reflecting that log on-ledger so it is replicated across all venue validator nodes. That happens automatically if non-compliant allocations are not let time out, but cancelled explicitly as part of batch settlement. | **Yes** | Recording denials on-ledger by cancelling explicitly. | Section 3.6 D1 paragraph. |
| 50 | [r4016319098](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016319098) | 657 | It seems important that this value is rendered and presented to the user upon signing. Consider calling it out here as an assumption on wallet providers. | **Yes** | The holder signs an external-chain destination the escrow acts on, and the recipient signs a preapproval with bounds; nothing said wallets must render them. | Section 3.3 step 1: wallet rendering assumption for the redemption and the preapproval. |
| 51 | [r4016334636](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016334636) | 793 | There are in principle no consumers other than the backend, which anyways needs to monitor the ACS. So the key is nice to have, not required. | **No** | Here keys are fetched on-ledger inside choices (mint, gateway, sweep) so that no caller supplies a contract id; they are load-bearing for section 3.4, not a convenience for a backend. | - |
| 52 | [r4016351029](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016351029) | 804 | I'm kind of unsure about the sample code here: it doesn't seem to have been battle-tested by writing an e2e test in Daml script against it for all flows of the DEX. I worry a bit that presenting this sketch confuses readers. I'd consider factoring this out, or at least marking it very clearly as a sketch. | **No** | This document contains no code sketch. | - |
| 53 | [r4016338090](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016338090) | 830 | Missing: `ContractId Holding` for assets | **No** | Pool template field; no analogue. | - |
| 54 | [r4016357409](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016357409) | 879 | missing: batch settlement | **No** | Delegation choice; no analogue. | - |
| 55 | [r4016383391](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016383391) | 933 | I've seen good ROI from writing negative tests, as done here: https://github.com/canton-network/splice/blob/main/daml/splice-amulet-test/daml/Splice/Scripts/TokenStandard/TestAmuletAllocationNegative.daml Consider asking also for these. I'm a bit wary about property-based testing, as (a) it seems difficult to setup for Daml script and (b) it may well give a false sense of security and (c) it may significantly increase test time w/o a corresponding increase in test coverage. | **Yes** | The component table listed daml-props (property-based) with no test strategy. | Section 1.3 validation row: negative tests for each fail-closed path first, property-based checks supplementary. |
| 56 | [r4016399037](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016399037) | 946 | The mitigation does not seem to cover the front-running behavior. That requires the auditor. Consdier also moving front-running into a separate Vector, as its mitigation is different from the state manipulation. | **No** | No ordering-sensitive price, so no front-running vector. | - |
| 57 | [r4016413157](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016413157) | 949 | Missing: Abuse `dvv` authority via SCU to a malicious package | **Yes** | The threat model had failed-rollout and unvetting rows but no row for a compatible upgrade that abuses the wTOK admin's or the Custodian's authority. | Section 4.3 'Malicious package upgrade' row; section 3.7 'Protecting the authority the packages carry'. |
| 58 | [r4016466981](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016466981) | 952 | the venue operator and the venue validators earning fees off of the acceptance of trades is another mitigation. A good business is made to run ;-) | **Yes** | Section 5.2 called for a fee without saying that fees are also an incentive control. | Section 5.2: fee as a mitigation. |
| 59 | [r4016478930](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016478930) | 1007 | Pool contract and the holding UTXOs for the pool reserves | **No** | No pool holdings. | - |
| 60 | [r4016491602](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016491602) | 1019 | only works if they have the same `SettlementInfo`. Otherwise multiple chained calls to `SettlementFactory_SettleBatch` need to be used to implement batch settlement. | **Yes** | 5.1 promised batch sharing without the same-settlement-id condition. | Section 5.1 bullet qualified; chained `SettlementFactory_SettleBatch` for different ids. |
| 61 | [r4016444711](https://github.com/OpenZeppelin/canton-specs/pull/198#discussion_r4016444711) | 1043 | I think there's a missing problem here: the venue operator pays extra traffic for which it should get paid. While they do get app rewards, these may not be sufficient to compensate them for their operations. Consider adding an explicit fee logic that accrues LP tokens to the operator (funded from part of the fees taken from traders). | **Yes** | The relayer pays nearly all traffic and 5.2 left the fee undefined. | Section 5.2 proposes a fee leg inside the settlement batch; section 6 gains a 'Fee model' open question. |
| 62 | [review 5209502816](https://github.com/OpenZeppelin/canton-specs/pull/198#pullrequestreview-5209502816) | - | Thanks for the good work. I think the design is mostly there except for some decisions wrt the party topology, and the presentation of said topology. | n/a | Review summary, no action. | - |

## PR #83 (https://github.com/OpenZeppelin/canton-specs/pull/83)

| # | Comment | `dex.md` line | Simon's comment | Relevant? | Why | Applied as |
|---|---|---|---|---|---|---|
| 63 | [r3969217937](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969217937) | 5 | What kind of reuse do you have in mind: 1. reference the libraries via their compiled .dars 2. vendor the code under a new package-name specific to the DEX Given that SCU is scoped to package names and package names are currently global, only Option 2 currently works. I'd suggest to call this out in an appropriate section in the doc. Note that we are working on a design for Canton to support Option 1, which is a much better and more standard option. That will though take a while to land on MainNet. So it's better to not rely on it for now. | **Yes** | The rail consumes shared OpenZeppelin packages and the settlement package; 3.7 never said how, and SCU lineage coupling applies verbatim. | Section 3.7 'Dependency packaging': vendor each dependency under the rail's own package name. |
| 64 | [r3976211512](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3976211512) (reply) | 5 | It works well if we are looking at a single application. It starts to become problematic if there are two applications A and B that have common users and both depend on `Pausable`. In that case, the upgrade lineages of `Pausable` are coupled across the two applications. If A decides to upgrade `Pausable`, then all users that are shared with B will also vet the upgraded version. They might not be able to use it in workflows with B though which is fine. The real problem starts when `B` actually wants to upgrade `Pausable` in a way that is conflicting with the upgrade of `Pausable` that `A` uses. In that case, the shared users will vet packages that are SCU-incompatible. They will only be able to do so using `--force` flag on vetting. Furthermore, the automatic package selection used on command submission is no longer guaranteed to work. What's missing from Canton is a first-class notion of different app instances and scoping all upgrade related actions to these app instances. That's what we're working on designing. In the meantime, the kind of sharing you envision is possible, but adds risk to the evolution of the apps that use it. That risk can be avoided by each app republishing the dependencies under their own package name. | **Yes** | Same thread, the coupling argument. | Section 3.7 'Dependency packaging'. |
| 65 | [r3969235449](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969235449) | 15 | > Ideally, all of this holds without either party having to trust the executor of the trade. Given me comment below this probably needs to be turned around: DEX needs to be implemented such that traders can trust it to always execute both legs of the trade at once. | **No** | Trust framing of a two-legged trade; the inbound credit has one leg and the recipient's trust is already framed as its own signature. | - |
| 66 | [r3969244336](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969244336) | 25 | If traders can trust the DEX, then the output amount does not need to be pinned. | **No** | The amount is fixed by the lock, so pinning is required, not optional. | - |
| 67 | [r3969259079](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969259079) | 36 | Do you also have an experiment where you implement a full DEX? In my experience, writing the actual Daml code and e2e tests greatly improves the design; and is a must have to validate it. | **Yes** | Section 1.3 said the boundary is unbuilt but did not name an end-to-end exemplar as the validation step. | Section 1.3: end-to-end Daml Script exemplar named as the first build step. |
| 68 | [r3969266254](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969266254) | 45 | In what sense do you provide "modular extensibility"? | **Yes** | Section 3.8 named substitution points without saying how substitution happens. | Section 3.8: compile-time substitution in the adopter's own package; runtime configuration in data records. |
| 69 | [r3976342304](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3976342304) (reply) | 45 | Given my other comment about managing upgrades, I believe it would be good for the design to contain a section about "Contract Deployment and Customization". I see multiple options: The compile-time customization axis: 1. deploy the dars as-is with the default functionality. Good for app providers that just want to run the stock DEX, and accept that they have to follow the upgrade path that you maintain for all of them. 2. vendor source code and recompile, but only change package names. The option for app providers that want to be able to opt out of the upgrade path and take control of their own. 3. vendor source code and change modules marked as intended to change; e.g., hooks implemented in a separate module. This should still make it easy to consume upstream changes. The runtime-customization axis 1. allow customization via built-in configuration records 2. allow customization via contracts whose interface methods implement custom code (hooks) I have seen Compile 2 and Runtime 1 being used well in practice. I have seen Compile 1 being used in a managed service offering. I'm unsure whether you will manage to streamline all requests for changes into a single SCU lineage. It would be great though if you could. I think one can make Runtime 2 work. It requires discipline though to work around the fact the interface definitions cannot (yet) be upgraded using SCU. So every new set of hooks requires a new interface definition in its own package. | **Yes** | Compile-time and runtime customization axes. | Section 3.8 (compile-time vendoring, data-record runtime config) and 3.7 (own package name). |
| 70 | [r3976355433](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3976355433) (reply) | 45 | My main worry about hooks is getting the security right. Daml compiles all template and choice references using static links, which simplifies reasoning about the possible transactions. Interfaces are resolved dynamically, which adds a lot more options for things to go wrong in a security sense. | **Yes** | The SCU worked example in 3.7 used an interface-dispatched 'hook' on the mint, the pattern Simon warns about. | Section 3.7 example reworded to a policy data record; section 3.8 states the payment path has no interface hooks. |
| 71 | [r3969289969](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969289969) | 53 | > sweep allocation funds to a preset custodian account This reads as if the funds in an allocation for a trade could be swept. Is that really the case? That seems surprising to me. Could it be that this is about LP tokens and reserves? Also: how do you keep track of who is the blocked owner of the funds held in custody? | **Yes** | D2 sweeps locked allocation holdings here as well, and nothing recorded whose funds the Custodian holds. | Section 3.6 D2: the sweep records the seized owner and the mark's reference in the swept holding's metadata. The 'is that really the case' part is already answered by the 'Seizure scope' paragraph. |
| 72 | [r3969308603](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969308603) | 54 | The target topology for parties is crucial for building a DEX. This description here feels too terse to get the details. Can you expand? (May be there's a later section that has more detail.) | **Already addressed** | Sections 2.1 and 2.3 already give the topology in detail; the rebuilt 2.3 table sharpens it further. | Section 2.3 table. |
| 73 | [r3976227158](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3976227158) (reply) | 54 | yeah, it does have more detail. See though my comments on it wrt what's missing there. In general, choosing the party topology is a key design choice, and warrants a clear explanation. | **Already addressed** | Same thread. | - |
| 74 | [r3969313202](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969313202) | 102 | 3.5.1 is out. When do you plan to do this migration? | **No** | No 'keyless today' statement; the document targets Daml-LF 2.3 on Protocol Version 35 explicitly. | - |
| 75 | [r3969338248](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969338248) | 109 | > per-party `TokenAllocationRequest` and `TokenAllocation` These references concern *implementation contracts*. I'd have expected these to refer to contracts implementing `V2.AllocationRequest` or `V2.Allocation`. | **No** | The prose already uses standard names (allocation, allocation request, `EventLog`); implementation names appear only in the component-location table. | - |
| 76 | [r3969355856](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969355856) | 138 | I'd use at least two separate token implementations. For example, Amulet and OpenZeppelinCIP112V1. There's also `TestTokenV2` that was built as part of TSv2 validation: https://github.com/canton-network/splice/blob/main/token-standard/examples/splice-test-token-v2/daml/Splice/Testing/Tokens/TestTokenV2.daml | **No** | wTOK's registry is the design's own instrument registry, and D1 and D2 are registry-side features of it, so there is no second registry to validate against. | - |
| 77 | [r3976235885](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3976235885) (reply) | 138 | Yes. I was reacting to your design text being hard-bound to your own test token implementation. | **No** | Same thread. | - |
| 78 | [r3969365123](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969365123) | 162 | base and quote do not necessarily have the same registrar! | **No** | Single instrument. | - |
| 79 | [r3969377632](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969377632) | 176 | All the above are represented by parties. Here you are talking about an account, why is that? | **No** | Roles are parties here; the custodian account is a destination and the Custodian party owns it. | - |
| 80 | [r3969423189](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969423189) | 225 | What do you think about separating this section into - subsection that explains the constraints on how parties are hosted and how their validation is configure - subsection that explains whether, and if yes how, transactions are submitted in the name of each party Thereby simplifying understanding the party topology and reasoning about its security guarantees.  I have a hunch that presenting the topology as a table would be helpful. Please also reuse the names for the parties that you defined earlier. | **Yes** | The posture table lacked a 'who submits in its name' dimension, and the off-ledger actors table carried it separately. | Section 2.3 table now has hosting and validation, organizations, and submission columns, using the party names of 2.1. |
| 81 | [r3969432564](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969432564) | 243 | AFAIS, the party topology contains a lot of design decisions and should thus also be part of "3. Target Design" | **Partial** | Topology decisions live in 2.3 under 'Architecture Overview'. Moving the section would renumber cross-references throughout and break the structure shared with the sibling reports. | Section 2.3 marks the posture table as the 'Decision' rather than moving it. |
| 82 | [r3969439471](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969439471) | 258 | What kind of API is this? Probably a custom HTTP API. Consider also giving an OpenAPI spec for it. | **No** | No off-ledger API is exposed to users; the relayer backend watches the external chain. | - |
| 83 | [r3969606921](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969606921) | 382 | missing: registries for base and quote asset wrong: AllocationFactory_Allocate does not call `SettlementFactory` note: - request for quote would likely not originate from the wallet, but from a venue UI that uses CIP-103 to interact with the wallet | **Yes** | Diagram accuracy: the inbound diagram had a wTOK ADMIN actor performing the mint, which is a registry step run under the admin's authority inside the relayer's settlement. | Inbound diagram: the admin actor is removed and the mint is drawn as the registry's step. |
| 84 | [r3969616881](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969616881) | 449 | As pointed out in a later comment: a single step swap is possible for all assets that support allocate + settle in the very same tx. Even for assets that do not do so, a two step flow is also possible. | **Yes** | The three relayer transactions can collapse into one; the design did not say so or why it keeps them apart. | Section 3.1 'Fewer relayer transactions'. |
| 85 | [r3969654214](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969654214) | 474 | I'd expect the tracking to be by `SettlementInfo.id`  You don't need to watch the completion stream. Instead ingest the ACS and act whenever you see a trade that can executed (i.e. the required allocations are available and match the state of the current `Pool`). In Splice, we code all of this as triggers. See here for the SV app triggers: https://github.com/canton-network/splice/tree/main/apps/sv/src/main/scala/org/lfdecentralizedtrust/splice/sv/automation/singlesv Also see this code for the TSv2 example TradingAppV2 that simulates the actions of the backend in Daml script: https://github.com/canton-network/splice/blob/main/token-standard/splice-token-standard-v2-test/daml/Splice/Testing/Apps/TradingAppV2_Backend.daml | **Yes** | 'Delivery and retry' described a command-id, completion-stream model. | Section 3.1: the relayer drives each step from the active contract set keyed by the nonce, the Splice trigger pattern. |
| 86 | [r3969679612](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969679612) | 481 | Not clear why we talk about deadlines in this detail for a DEX. AFAIS, the only usage of time is that it is up to the trader to determine the expiry of their request for a trade. It seems like expiry being slightly fuzzy is not a problem. | **No** | Deadlines are load-bearing here: refund exclusivity depends on the settlement deadline sitting inside the attestation's validity. | - |
| 87 | [r3969686901](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969686901) | 523 | note: `TokenAllocation` -- search and replace, and ensure that base and quote can be from different registries | **No** | No implementation-name usage in prose. | - |
| 88 | [r3969702736](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969702736) | 531 | Did you consider using a transfer from the special `cip-112/mint` account? Also consider having the LP create an allocation that authorizes the receipt, which allows doing the minting as a normal settlement. | **Yes** | The mint created holdings that the settle archived a step later; CIP-0112's `cip-112/mint` account expresses the mint as a standard leg. | Section 3.2 'Supply creation' prefers the `cip-112/mint` account. |
| 89 | [r3969708169](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969708169) (reply) | 531 | and then the same for the burn below. See https://github.com/canton-foundation/cips/blob/main/cip-0112/cip-0112.md#4321-special-account-identifiers-for-mint-and-burn | **Yes** | Same for the burn. | Section 3.3 step 1: `cip-112/burn` account as an equal shape. |
| 90 | [r3969748195](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969748195) | 662 | If you go for a decentralized `dvo` party, then you'll also need to describe how you protect the upgrade of the contracts that carry its authority. You'll want a multi-sig setup that is trustworthy and ensures no upgrade is deployed that abuses that authority. Currently you'll need to achieve that by controlling which upgrades the hosting validator nodes accept. | **Yes** | A decentralized wTOK admin needs a protected upgrade path for the contracts that carry its authority; 3.7 had none. | Section 3.7 'Protecting the authority the packages carry' and the 4.3 row. |
| 91 | [r3969770322](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969770322) | 757 | it's not clear that both base and quote support the same account structures. If you do use accounts then you need one for each asset. In general you probably want to define ``` data PoolAssetState with ... ``` and then add `baseState : PoolAssetState` and `quoteState : PoolAssetState` fields. Thereby sharing the fields and code. | **No** | Pool data record; no analogue. | - |
| 92 | [r3959686725](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3959686725) | 774 | I'd have expected two settlement factories to be passed in: one to settle asset `x` movements and another one to settle asset `y` movements. | **No** | Two settlement factories; single instrument. | - |
| 93 | [r3959764960](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3959764960) | 892 | I'm not sure this holds when the two assets are administrated by different parties. In these cases, the `executors` of the allocations are trusted to ensure cross-registry atomicity. AFAIS, you want to avoid that the `venue` party is trusted with this. That makes sense to me. What seems to be missing though is to distinguish between the party that tracks and validates the venues state changes, and the party that represents the off-ledger component choosing when to perform what state change.  So, if you introduced a decentralized venue operator party (`dvo` in the following text) and make that the signatory of all contracts that represent the venue's state, then you can organize your deployment similar to how Canton Coin or cBTC are organized: decentralize the `dvo` across a number of participant nodes and thus have them jointly provide the guarantee that the venue operates as per its Daml contracts. Delegations from the `dvo` to a `venue` party can then be used to allow the `venue` party to drive the actions of the `venue`. | **Partial** | The design already separates the party that signs the state (wTOK admin) from the party that drives it (relayer). The Canton Coin and cBTC precedent and the delegation idea were not cited. | Section 2.3 multi-hosted bullet cites the precedent. |
| 94 | [r3969577271](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3969577271) (reply) | 892 | Yes your understanding is correct. One can view the `dvo` party and its hosting participant nodes as building a dedicated validator-set for a particular application. The validators in there need to be trusted in aggregate for integrity (Daml tx validation) and each on their own for confidentiality. To make this validators trustworthy one wants to ensure that they have the right incentives: - they should earn decent money for doing their work - they should have reputation to loose if they leak confidential details - they should be effectively governed, so that misbehavior is punished (e.g., by loosing reputation and money) One option for getting these validators is to host the party on covalidation service providers. For example, DA is providing such a service: https://docs.digitalasset.com/covalidation/overview . It also looks like the network may further incentivize such services via validator rewards: https://lists.sync.global/g/cip-discuss/topic/cip_0120_traffic_based/120502087 As you noticed, designing the venue to be trustworthy also gives greater flexibility in the design; and address compliance-concerns venue-side independent of the registries that you interact with. | **Yes** | Hosts trusted in aggregate for integrity and individually for confidentiality is a privacy consequence this design must state, since the wTOK admin sees every payment. Incentives were unstated. | Section 2.3 multi-hosted bullet; section 5.2 incentives. |
| 95 | [r3959881496](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3959881496) | 1079 | I think there are options to reduce the number of transactions required. The most extreme version is the one where the venue uses CIP-103 (dApp API) to directly prepare the swap transaction which creates the allocations and settles them in the very same transaction. This requires explicit disclosure for the input holdings covering the reserve amounts and the `Pool` contract. It has the advantage that the trader carries the full traffic cost and the venue gets app rewards. The drawback is that it suffers from contention, and is thus only accessible to traders that can sign quickly enough. This design also requires to invert the approach wrt attestations: instead of the expensive one-off attestations, attestations have a longer lifetime and are actively revoked if the user is sanctioned. The alternative is for the venue to use CIP-103 to create the allocations and thus skip over the allocation request. Furthermore, instead of the pool account creating allocations in a separate step, the pool allocations can be created in the settlement transaction. Thereby saving another transaction. An example for creating allocations as part of settlement can be found in TSv2 example trading app. It auto-creates allocations for the receipt of funds for traders that only have V1 wallets: https://github.com/canton-network/splice/blob/6b27921ed7d7868a76b7ddcd598edf8072e0ed78/token-standard/examples/splice-token-test-trading-app-v2/daml/Splice/Testing/Apps/TradingAppV2.daml#L187-L198 | **Partial** | Transaction-count reduction applies. The long-lived, revocable attestation alternative is not adopted: D1 is single-use by requirement and D3 already provides the long-lived credential. | Section 3.1 'Fewer relayer transactions'. |
| 96 | [r3959800164](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3959800164) | 1117 | If the trader trusts the venue (e.g., because it is properly decentralized), then you could transport the `minOut` amount in the metadata of the `input` allocation. The output allocation itself would be an iterated allocation with no funds attached. It is required to approve the receipt of funds. | **No** | The amount is fixed by the lock, so the receive-side allocation is committed; there is no min-out. | - |
| 97 | [r3970261374](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3970261374) (reply) | 1117 | > If there is any change remaining out of the trader's amountIn, the CIP0112 implementation should make sure to create an allocation with the change, which the trader can withdraw later. If you need to be able to return change, then I'd suggest to also use an iterated allocation on the input leg. This way you can settle the actual transfer, and change is automatically returned. | **No** | No change to return. | - |
| 98 | [r3959811321](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3959811321) | 1130 | What kind of throughputs do EVM pools see? With an average prepare-submission delay of 15', the current design only supports 4 swaps per hour! | **Yes** | 4.5 named the serialization point without a throughput formula or a way to raise the ceiling. | Section 4.5: credits per settlement over commit latency, raised by batching. |
| 99 | [r3970383198](https://github.com/OpenZeppelin/canton-specs/pull/83#discussion_r3970383198) (reply) | 1130 | If you have the authority of the sender and receiver available, then you can use a direct transfer with the actors set to sender and receiver. This SHOULD work for V2 assets as per https://github.com/canton-network/splice/blob/411cfcdc0036b2adcfa646f65f284ff9b1899441/token-standard/splice-api-token-transfer-instruction-v2/daml/Splice/Api/Token/TransferInstructionV2.daml#L242-L244 However: this only works for assets in accounts that are self-custodied. If an asset is held in a way that requires transfers to be authorized by a third-party (e.g. custodian), then you'd also need the authority of the custodian. That's where allocations come in: these separate the process to grant the authority for the transfer to happen from the time where the actual transfer happens. That's also why they need at least two transactions: one to create the allocations and another one to settle them. > In light of this understanding, and the fact that transfers do not require the extra steps of creating the allocations (and hence might increase throughput) Note that doing multiple steps in the same transaction does not meaningfully change the latency of its commit time. Your main throughput limitation comes from the latency of the "critical section" in the DEX. If you allow the settlement of allocations to be reordered (i.e., don't fix the amounts up-front and instead limit slippage), then you can even do batch settlement of orders. Your throughput will be `batch-size / MainNet-commit-time`, thus at batches of 100 swaps and a commit time < 5s, you can settle 20 swaps/s. | **Yes** | Allocations separate granting authority from moving funds, and batch settlement sets throughput. | Section 4.5 and the new bullet in 'Settlement over a direct mint'. |
| 100 | [review 5143881336](https://github.com/OpenZeppelin/canton-specs/pull/83#pullrequestreview-5143881336) | - | Thanks a lot for this valuable write-up. I've read the full text, but did not get to adding all comments that I wanted to add. The comments in this review do though seem to be core to the design, which is why I'm sharing them early. | n/a | Review summary, no action. | - |
| 101 | [review 5155266509](https://github.com/OpenZeppelin/canton-specs/pull/83#pullrequestreview-5155266509) | - | Review pass number two. | n/a | Review summary, no action. | - |

## Totals

| Verdict | Count |
|---|---|
| Yes | 53 |
| Partial | 5 |
| No | 38 |
| Already addressed | 2 |
| Review summaries (n/a) | 3 |
| All rows | 101 |

