# Placeholder only.
# Do not rename this file to daml.yaml until the SDK/CIP pinning ADR is
# accepted and the selected package manager path is available for local manual
# workflow tests.

sdk-version: "<accepted-daml-sdk-version>"
name: oz-canton-specs
source: daml
version: 0.0.0
dependencies:
  - daml-prim
  - daml-stdlib
